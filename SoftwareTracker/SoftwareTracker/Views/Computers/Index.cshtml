@model IPagedList<EvidencijaSoftvera_IlijaDivljan.Models.Computers>

@{
    ViewBag.Title = "Index";
}

<h2>Registered Computers</h2>

<p>
    @Html.ActionLink("Register new", "Create")
</p>

<form method="GET" action="@Url.Action("Index")" data-es-ajax="true" data-es-target="#computersList">
    @*ind by name: <input type="search" name="searchTerm" data-es-autocomplete="@Url.Action("Autocomplete")" />
        <input type="submit" class="btn btn-default btn-sm" value="Search"/>*@

    <div class="row">
        <div class="col-lg-6">
            <div class="input-group">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                </span>
                <input type="search" name="searchTerm" class="form-control" placeholder="Search for name..." data-es-autocomplete="@Url.Action("Autocomplete")">
            </div>
        </div>
    </div>

</form>
<br />

@Html.Partial("_Computers", Model)



