@using Microsoft.AspNet.Identity
@model EvidencijaSoftvera_IlijaDivljan.Models.InstalledSoftware

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@if (Model.ApplicationUserId != User.Identity.GetUserId() && !User.IsInRole("Admin"))
{
    <p class="alert alert-danger">You are not authorized to edit other Users records.</p>
}
else
{
    using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4>Installed Software</h4>
            <hr/>
            @Html.ValidationSummary(true, "", new {@class = "text-danger"})
            @Html.HiddenFor(model => model.InstalledSoftwareId)

            <div class="form-group">
                @Html.LabelFor(model => model.RecordDate, htmlAttributes: new {@class = "control-label col-md-2"})
                <div class="col-md-10">
                    @Html.EditorFor(model => model.RecordDate, new {htmlAttributes = new {@class = "form-control"}})
                    @Html.ValidationMessageFor(model => model.RecordDate, "", new {@class = "text-danger"})
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-md-2">Computer</label>
                @*@Html.LabelFor(model => model.ComputersId, "ComputersId", htmlAttributes: new {@class = "control-label col-md-2"})*@
                <div class="col-md-10">
                    @Html.DropDownList("ComputersId", null, htmlAttributes: new {@class = "form-control"})
                    @Html.ValidationMessageFor(model => model.ComputersId, "", new {@class = "text-danger"})
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-md-2">Sofware</label>
                @*@Html.LabelFor(model => model.SoftwareId, "SoftwareId", htmlAttributes: new {@class = "control-label col-md-2"})*@
                <div class="col-md-10">
                    @Html.DropDownList("SoftwareId", null, htmlAttributes: new {@class = "form-control"})
                    @Html.ValidationMessageFor(model => model.SoftwareId, "", new {@class = "text-danger"})
                </div>
            </div>

            @*<div class="form-group">
                <label class="control-label col-md-2">User</label>
                <div class="col-md-10">
                    @Html.DropDownList("ApplicationUserId", null, htmlAttributes: new {@class = "form-control"})
                    @Html.ValidationMessageFor(model => model.ApplicationUserId, "", new {@class = "text-danger"})
                </div>
            </div>*@

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Save" class="btn btn-default"/>
                </div>
            </div>
        </div>
    }
}

<div>
    @Html.ActionLink("Back to List", "Index", new { selectedComputer = Session["selectedComputer"] })
</div>

@section Scripts {
}
